# Component Tree Demo

Simple Vite + React demo showing props passed from App → Main → Content → PostList → PostItem.

Run locally:

```bash
npm install
npm run dev
```

Open http://localhost:5173
# nd19999999